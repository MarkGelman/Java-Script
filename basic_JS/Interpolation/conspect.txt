Интерполяция -- это возможность вставлять внутри строки вставлять значение переменной

                console.log(`https://somerul.com/${category}`);

    `string${category}` -- это синтаксис + вся строка должна быть взята в `` (ляптики
                            косые кавычки)           